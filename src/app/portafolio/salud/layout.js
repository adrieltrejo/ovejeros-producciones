export const metadata = {
  title: 'Salud y Bienestar | Portafolio Ovejeros',
  description: 'Contenido corporativo, video y fotografía especializada para el sector médico, clínicas y bienestar.',
};

export default function Layout({ children }) {
  return children;
}
